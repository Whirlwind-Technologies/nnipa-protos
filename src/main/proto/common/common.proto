syntax = "proto3";

package com.nnipa.proto.common;

option java_package = "com.nnipa.proto.common";
option java_outer_classname = "CommonProtos";
option java_multiple_files = true;

import "google/protobuf/timestamp.proto";
import "common/enums.proto";

// ============================================
// Common Messages
// ============================================

// Base event metadata included in all events
message EventMetadata {
  string event_id = 1;                      // Unique event identifier
  string correlation_id = 2;                // For tracking related events
  string source_service = 3;                // Service that generated the event
  google.protobuf.Timestamp timestamp = 4;  // Event creation time
  string version = 5;                       // Schema version
  map<string, string> headers = 6;          // Custom headers
  string tenant_id = 7;                     // Multi-tenant context
  string user_id = 8;                       // User who triggered the event
  Priority priority = 9;                    // Event priority
  int32 retry_count = 10;                   // Number of retries
  string event_type = 11;                   // Event type classification
}

// Standard error information
message ErrorDetail {
  string code = 1;
  string message = 2;
  string details = 3;
  google.protobuf.Timestamp timestamp = 4;
  string service = 5;
  map<string, string> metadata = 6;
}

// Command response wrapper
message CommandResponse {
  bool success = 1;
  string message = 2;
  ErrorDetail error = 3;
  google.protobuf.Timestamp timestamp = 4;
  string correlation_id = 5;
}

// Pagination information
message PaginationInfo {
  int32 page = 1;
  int32 page_size = 2;
  int64 total_items = 3;
  int32 total_pages = 4;
}

// Audit information
message AuditInfo {
  string created_by = 1;
  google.protobuf.Timestamp created_at = 2;
  string updated_by = 3;
  google.protobuf.Timestamp updated_at = 4;
  string ip_address = 5;
  string user_agent = 6;
}

// Address information
message Address {
  string street_line1 = 1;
  string street_line2 = 2;
  string city = 3;
  string state_province = 4;
  string postal_code = 5;
  string country = 6;
  double latitude = 7;
  double longitude = 8;
}

// Contact information
message ContactInfo {
  string email = 1;
  string phone = 2;
  string mobile = 3;
  string fax = 4;
  Address address = 5;
}

message StorageMetadata {
  string object_id = 1;
  string tenant_id = 2;
  string bucket_name = 3;
  string region = 4;
  string storage_class = 5;
  int64 size_bytes = 6;
  string content_type = 7;
  string checksum = 8;
  google.protobuf.Timestamp created_at = 9;
  google.protobuf.Timestamp modified_at = 10;
  map<string, string> custom_metadata = 11;
}

// Common command metadata
message CommandMetadata {
  string command_id = 1;
  google.protobuf.Timestamp timestamp = 2;
  string source = 3;
  string target = 4;
  string correlation_id = 5;
  string user_id = 6;
  map<string, string> headers = 7;
}

// Common error response
message ErrorResponse {
  string error_id = 1;
  string error_code = 2;
  string message = 3;
  google.protobuf.Timestamp timestamp = 4;
  map<string, string> details = 5;
}